import { createTheme } from "@mui/material/styles";

const appTheme = createTheme({
  palette: {
    primary: {
      main: "#32B768",
    },
    primarylighter: {
      main: "#91C788",
    },
    secondary: {
      main: "#FF8473",
    },
  },
});

export default appTheme;
